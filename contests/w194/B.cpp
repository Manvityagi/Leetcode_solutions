class Solution
{
public:
    vector<string> getFolderNames(vector<string> &names)
    {
        unordered_map<string, int> mp;
        int n = names.size();
        vector<string> ans(n);
        for (int i = 0; i < n; i++)
        {
        }

        return ans;
    }
};